# Public API keys - Used for normal client authentication 
VITE_SUPABASE_URL=Your_Supabase_URL
VITE_SUPABASE_ANON_KEY=Your_Supabase_Public_API_Key

# Admin-only keys - Required for Super Admin functionality
# ⚠️ WARNING: This key has admin powers and should never be exposed to the public or committed to version control
# Only add this key to your local .env file if you need Super Admin access
VITE_SUPABASE_SERVICE_ROLE_KEY=Your_Supabase_Service_Role_Key

# SMTP Configuration for Email sending
# These should be set as Supabase Edge Function secrets
# Run: supabase secrets set SMTP_HOST=smtp.example.com SMTP_PORT=465 ...
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_NAME=GUB EMS
SMTP_FROM_EMAIL=your-email@gmail.com
